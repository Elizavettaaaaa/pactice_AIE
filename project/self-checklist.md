# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| # | Критерий | Да/Нет (студент) | Где смотреть / комментарий |
|---|----------|------------------|---------------------------|
| 1 | Сервис запускается по инструкциям из `project/README.md` и работает | ✅ | `project/README.md`, раздел «Как запустить проект». Проверено на macOS через `uvicorn` и Docker. |
| 2 | Endpoint `/predict` использует **реальную модель**, а не заглушку | ✅ | `src/service/app.py`, `src/service/model_loader.py`. Модель: `models/churn_model.pkl` (XGBoost tuned). |
| 3 | Есть EDA и хотя бы один эксперимент с метриками | ✅ | `notebooks/01_Telco_EDA_and_Preprocessing.ipynb` – EDA, анализ пропусков, корреляций, визуализации. Базовый эксперимент с логистической регрессией. |
| 4 | Есть baseline и улучшенная модель, есть **сравнение по метрикам** | ✅ | `notebooks/02_Telco_Model_Training.ipynb` – обучены 6 моделей, сравнение по Accuracy, Precision, Recall, F1-score, ROC-AUC. Лучшая: XGBoost (tuned) с F1=0.635. |
| 5 | Код не свален в один ноутбук: есть внятная структура в `src/` | ✅ | `src/service/app.py`, `src/service/model_loader.py`. Код разделён на модули. |
| 6 | Есть Dockerfile **или** понятный сценарий развёртывания без Docker | ✅ | `docker/Dockerfile`, `docker/docker-compose.yml`. Инструкция в `README.md`. |
| 7 | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей | ✅ | `project/.env.example`. Секреты не закоммичены, проверено через `git status --ignored`. |
| 8 | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`) | ✅ | `src/service/app.py`: логирование через `logging`, эндпоинт `/health`, метрики Prometheus (`/metrics`). |
| 9 | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | `report.md`, разделы 4.3 (Выбор финальной модели) и 5.3 (Trade-off между Recall и Precision). |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅ | `README.md` – инструкция по запуску, примеры запросов. `report.md` – раздел 9 «Сценарий демонстрации на защите». |

---

## Подсчёт баллов и связь с оценкой

За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.  
Максимум – 10 баллов.

Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:

- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).

Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.

---

**Итого:** выполнено **10 из 10** критериев.  
**Предварительная оценка: 5 (отлично).**